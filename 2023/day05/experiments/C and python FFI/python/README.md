Python Ctypes experiment
------------------------

# Build / Run

There is a [build lib](../c_lib_src/lib/) in the `c_lib_src` directory which contains the C library, this is loaded at the top of the file.

```
./day5.py [input_file] [run]
```

Where [run] is:
* part_a
* part_b